# 커밋 메시지 작성 양식

이 저장소의 모든 커밋은 아래 양식을 따른다. 기계 검증은 `commitlint.config.cjs`가 담당한다.
"어디에(scope) 무엇을(type)" 순서로 읽히도록, 표준 Conventional Commits(type 먼저)에서 **순서를 뒤집었다.**

## 기본 구조

```
<scope>(<type>): <제목>

<본문>

<꼬리말>
```

- **제목 줄**: `scope(type): 제목` 형식, 50자 이내 권장, 마침표 없음. **scope·type 둘 다 필수.**
- 제목과 본문 사이, 본문과 꼬리말 사이는 **빈 줄 한 줄**로 구분
- **본문**: 무엇을·왜 변경했는지 설명 (어떻게는 코드로 보여줌), 한 줄 72자 이내
- **꼬리말**: 이슈 번호, 공동 작성자 등 (선택)

## type 목록

| type | 용도 |
|------|------|
| `feat` | 새로운 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서 변경 |
| `style` | 코드 동작에 영향 없는 포맷팅 (공백, 세미콜론 등) |
| `refactor` | 기능 변경 없는 코드 구조 개선 |
| `perf` | 성능 개선 |
| `test` | 테스트 추가/수정 |
| `build` | 빌드 시스템, 의존성 변경 |
| `ci` | CI 설정 변경 |
| `chore` | 그 외 잡무 (설정, 스캐폴딩 등) |

## scope 목록

이 프로젝트의 디렉터리 구조 기준. (commitlint이 이 목록만 허용)

| scope | 범위 |
|-------|------|
| `root` | 레포 루트 협업 툴체인 (husky·commitlint·prettier·eslint·package.json 등) |
| `infra` | Docker, K8s, 로드테스트 |
| `front` | 프론트엔드 |
| `back` | 백엔드 전체 또는 공용 |
| `gateway` | API 게이트웨이 |
| `queue` | 대기열 서비스 |
| `reservation` | 예약/재고 서비스 |
| `payment` | 결제 서비스 |
| `figma` | 디자인 스펙·컴포넌트 |
| `docs` | 문서, 디자인 스펙 |

> scope는 필수다. 여러 곳에 걸치면 가장 핵심인 곳을 고른다.

## 예시

```
queue(feat): 대기열 진입 토큰 발급 API 추가

Redis 정렬 집합으로 대기 순번을 관리하고, 순번이 도래하면
입장 토큰을 발급한다. 토큰은 게이트웨이의 가드에서 검증된다.
```

```
reservation(fix): 재고 차감 시 동시성 경합 해결

Lua 스크립트로 재고 조회·차감을 원자적으로 처리해
초과 예약이 발생하던 문제를 수정한다.
```

```
docs(docs): 아키텍처 및 브랜칭 전략 문서 추가
```

## 규칙 요약

1. 제목은 명령형 현재 시제로 작성 (예: "추가한다" 보다 "추가")
2. 하나의 커밋은 하나의 논리적 변경만 담는다
3. 제목만으로 변경 내용이 드러나게 작성한다
